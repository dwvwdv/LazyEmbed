name = "lazyembed"
compatibility_date = "2024-01-01"
main = "src/worker.js"

# 靜態資產配置
[assets]
directory = "."
binding = "ASSETS"

# 部署配置
[env.production]
name = "lazyembed"
routes = [
  { pattern = "lazyembed.workers.dev", zone_name = "" }
]
